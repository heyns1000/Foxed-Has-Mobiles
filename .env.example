# Copy this to .env and fill values locally (DO NOT commit .env)
# Client (Vite) variables must be prefixed with VITE_
VITE_EXCHANGE_API_KEY=

# Server-side (Node) variables (DO NOT prefix)
EXCHANGE_API_KEY=
OTHER_SERVER_SECRET=
